{
  "name": "alpha-grid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sass --watch src/agrid/agrid.scss src/agrid/agrid.css",
    "build": "sass --style=compressed --no-source-map src/agrid/agrid.scss build/agrid.css",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "sass": "^1.49.8"
  },
  "devDependencies": {
    "husky": "^7.0.0"
  }
}
